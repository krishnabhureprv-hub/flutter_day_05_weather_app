name: weather_app_advanced
description: "A premium weather app using custom animations."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  fl_chart: ^0.66.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.6
  http: ^1.6.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true