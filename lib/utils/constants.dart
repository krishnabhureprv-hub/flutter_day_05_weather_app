import 'package:flutter/material.dart';

class AppConstants {
  static const String apiKey = '#';   //use your own api key i removed my keys because i dontwanna pay extra bills
  static const Color primaryColor = Color(0xFF1D3354);
  static const Color secondaryColor = Color(0xFF467599);
}